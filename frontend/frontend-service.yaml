apiVersion: v1
kind: Service
metadata:
  name: digilians-frontend-service
spec:
  selector:
    app: digilians-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
