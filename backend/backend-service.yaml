apiVersion: v1
kind: Service
metadata:
  name: digilians-backend-service
spec:
  selector:
    app: digilians-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080   
  type: NodePort
